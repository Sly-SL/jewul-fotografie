import AboutPage from "@/shared/components/custom/header/subdivs/subdivs/about/about.about";

export const dynamic = "force-static"

const Page = () => {
    return (
        <AboutPage/>
    );
};

export default Page;